////////////// COMPONENTS ///////////////////////
    - seperation of concern, reusability, maintainable

1. Custom components
    1.1. Its a normal js fn + jsx syntax(declarative) + export
    1.2. Import it into respective components
    1.3. pass data - between components --> use props 
    1.4. props
        1.4.1. @source a.k.a Parent --> attributes(key) + hardcoded/{var}(value)
        1.4.2. @destination a.k.a child... 
            --> props as label i.e. parameter in child fn 
            --> props.attribute name specified at source - Custom
            --> props.children -- used when we required to use content inside our custom tag
            --> props.className -- used when we required to use content inside our custom tag
    1.5. styles
        1.5.1. Normal way - specify css in a file + import as required
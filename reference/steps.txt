////////////// COMPONENTS ///////////////////////
    - seperation of concern, reusability, maintainable

1. Custom components
    1.1. Its a normal js fn + jsx syntax(declarative) + export
    1.2. Import it into respective components - static component
    1.3. styles
        1.3.1. Normal way - specify css/scss in a file + import as required
    1.4. pass dynamic value in a component - {}
    1.5. pass data - between components --> use props 
        1.5.1. @source a.k.a Parent --> attributes(key) + hardcoded/{var}(value)
        1.5.2. @destination a.k.a child... 
            1--> props as label i.e. parameter in child fn 
            2--> props.attribute name specified at source - Custom
            3--> spliting components - multi level data passing using props.
            4--> props.children -- used when we required to use content inside our custom tag
            5--> props.className -- used when we required to use content inside our custom tag    